# Copy this file to `.env` and fill in your key.
# Used ONLY by scripts/build_dataset.py (the local distillation step).
# On Kaggle you don't use this file — you add the key as a Kaggle Secret instead.
ANTHROPIC_API_KEY=your_key_here
